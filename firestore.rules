# Firestore Security Rules
# Firestore stores ONLY sync pair configuration (settings backup).
# No file data or file metadata is stored here — files transfer directly
# over local WiFi between devices.
# Deploy with: firebase deploy --only firestore:rules

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Users can only read/write their own data
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;

      // Sync pair config — settings only, no file records
      match /syncPairs/{pairId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
    }
  }
}
